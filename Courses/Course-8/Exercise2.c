#include <stdio.h>
#pragma warning(disable : 4996)
int main()
{
    // int arr[5];
    // double sum = 0;

    // for (int i = 0; i < 5; i++)
    // {
    //     printf("Enter a number: ");
    //     scanf("%d", &arr[i]);
    // }

    // for (int i = 0; i < 5; i++)
    // {
    //     sum += arr[i];
    // }

    // double average = sum / 5;

    // int min = arr[0];
    // int max = arr[0];

    // for (int i = 0; i < 5; i++)
    // {
    //     if (min > arr[i])
    //     {
    //         min = arr[i];
    //     }
    //     if (max < arr[i])
    //     {
    //         max = arr[i];
    //     }
    // }
    // printf("average: %g\nmax: %d\nmin: %d", average, max, min);

    return 0;
}